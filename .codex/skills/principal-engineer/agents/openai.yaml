interface:
  display_name: "Principal Engineer"
  short_description: "Action-first principal engineering"
  default_prompt: "Use $principal-engineer to execute this repository task through baseline, proven mechanism, the smallest safe change, actual verification, final diff inspection, and an explicit stop condition."
