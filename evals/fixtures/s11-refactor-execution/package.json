{
  "name": "s11-refactor-execution",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "for file in src/*.js; do node --check \"$file\" || exit 1; done",
    "test": "node --test test/*.test.js",
    "verify": "npm run check && npm test"
  }
}
